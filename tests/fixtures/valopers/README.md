# Valopers fixture provenance

These minimal, secret-free render fragments model the public Testnet 13
`gno.land/r/gnops/valopers` qrender contract. Public operator addresses,
monikers, and consensus keys are intentionally synthetic so tests remain
stable; no credentials, headers, signatures, or private material are present.
Live response compatibility still requires read-only validation on exp2.
