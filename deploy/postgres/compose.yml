services:
  postgres:
    image: postgres:16.14-bookworm
    container_name: utsa-gno-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-utsa_gno_indexer}
      POSTGRES_DB: ${POSTGRES_DB:-utsa_gno_explorer}
      POSTGRES_PASSWORD_FILE: /run/secrets/postgres_password
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - type: bind
        source: ${POSTGRES_DATA_DIR:-/var/lib/utsa-gno-explorer/postgres}
        target: /var/lib/postgresql/data
    secrets:
      - postgres_password
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\" -h 127.0.0.1 -p 5432"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 20s

secrets:
  postgres_password:
    file: /etc/utsa-gno-explorer/postgres-password
