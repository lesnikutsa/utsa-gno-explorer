# Install the real production file as /etc/utsa-gno-explorer/rpc.env.
# Treat it as sensitive because production RPC URLs may contain credentials.
GNO_CHAIN_ID=sapphire-1
GNO_RPC_URLS=https://rpc.sapphire.testnets.gno.land
RPC_MAX_HEIGHT_LAG=10
